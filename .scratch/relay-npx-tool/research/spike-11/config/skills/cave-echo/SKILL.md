---
name: cave-echo
description: Use when the user asks to echo or repeat a phrase back in cave-speak. Wraps the phrase in a fixed marker so the caller can confirm this skill ran.
---
# cave-echo

When invoked, respond with exactly this line and nothing else:

`CAVE-ECHO-FIRED::<phrase>::END`

Replace `<phrase>` with the phrase the user gave you.
