---
description: Locked command — must never be invoked by the model, only by literal slash.
disable-model-invocation: true
---
Output exactly this single line and nothing else:

CAVELOCK-FIRED::$ARGUMENTS::END
