<!-- wayfinder:map -->

# Map: relay npx tool

## Destination

A locked spec + resolved decisions for `@quantum-hub/relay` — a distributable npx tool (built + published via GitLab CI kipu commons components) that runs **one** pass over **one** Jira work item and hands off to a human.

- Input: a work-item key, or — with no param — the next `ready-for-agent` **Story / Bug / Vulnerability** for the current repo. A **Task** is never auto-picked; a Task passed as param exits with `error`.
- The pass is orchestrated as **Sandcastle-dispatched proper subagents per role** (planner, per-ticket implementer under TDD, per-ticket fast code + spec review, fixer, whole-branch in-depth code + spec review, fixer, quality-gate, commit) — the harness is the orchestrator, not one umbrella `kipu-afk` agent.
- Runs on a branch + worktree in a local Docker sandbox.
- Pilot target: **qc-catalog** (Java 21 / Maven). One repo first.

Plan only: this map produces the spec + decisions, then hands off to a build effort. It does not build the tool.

## Notes

**Build base — rebuild from scratch on the author's work:**

- The tool is built from the **Sandcastle author's more sophisticated templates + examples**: `@ai-hero/sandcastle` (local `/Users/michael.wurster/work/sandbox/sandcastle`, its own `.sandcastle/`) and `/Users/michael.wurster/work/sandbox/course-video-manager/.sandcastle/` (rich `agent-workflows/`, `run-with-retry`, `run-with-extraction`, `review/`, `update-branch/`), plus `matchplaytime`. These are the structural base.

**Prior art — reference for decisions only, NOT a codebase to extend:**

- The qc-catalog spike: `/Users/michael.wurster/work/sandbox/sandcastle_qc-catalog/.sandcastle/` — a working but throwaway per-repo prototype (host-owns-Jira poll loop, Docker CI-parity image, green-gate, credential model, glab MR). It is **Task-level, single-implement, poll-loop**. Use it to learn what worked and what didn't (e.g. the headless skill-expansion finding) — do **not** build on it.
- ADRs at `/Users/michael.wurster/work/kipu/skills/docs/adr/` — 0003 (afk story orchestration), 0004 (Sandcastle poll-claim harness contract + **headless Atlassian MCP auth proven**), 0005/0006 (per-ticket vs story model), 0007/0008 (review lens split). Treat these as locked context, not open questions.
- Publish reference: `/Users/michael.wurster/work/kipu/qhub-api/.gitlab-ci.yml` uses `gitlab.com/kipu-all/commons/pipeline-components/{compile-typescript,publish-npm,semantic-release}`.

**Skills the build will use (extracted into harness-dispatched subagents, not run as one umbrella):** kipu-commit, kipu-code-review, kipu-spec-review, kipu-mr, `tdd`. The implementer uses a **custom prompt** derived from the leaner `implement` skill (`kipu/skills/.agents/skills/implement`) minus its review line, not `kipu-implement`. kipu-afk is the *reference flow* only.

**relay sunsets `kipu-afk` and `kipu-implement` when live** — relay is their replacement, not a consumer (decided in ticket 07).

**Locked at chart time (from grilling):**

- Plan-only destination; hand to a build effort afterward.
- Package/command name is `relay` (renamed from `sandcastle` to avoid colliding with the upstream `@ai-hero/sandcastle` library it is built on); NPM scope `@quantum-hub`.
- Pilot on qc-catalog (Java/Maven); generalization to other repo types is out of scope.
- Hard constraint: orchestration lives in the harness; each role is a first-class `sandcastle.run()` so spawning skills work — inverting kipu-afk's inline-rubric compromise.
- Hard constraint: rebuild from scratch on the author's templates/examples; the qc-catalog spike is reference/prototype only, never a base.
- Recommended build approach (a decision, not a step here): bootstrap via the `sandcastle` CLI, then build up from the author's example workflows.

**Every session:** consult the prior art above; use `/grilling` + `/domain-modeling` for grilling tickets; use a `/research` subagent for research tickets.

## Decisions so far

<!-- one line per closed ticket; empty until tickets resolve -->

- [Sandcastle CLI + framework capabilities](issues/02-sandcastle-cli-capabilities.md) — `sandcastle` CLI only bootstraps (`init` + `docker build-image`); no `run` — our tool is our own TS orchestration on the library API (`run`/`createSandbox`/`claudeCode`/`Output.object`/`branchStrategy: branch`); Jira is not built-in (ours to wire); pin the pre-1.0 version, add zod, UID-matched image.
- [Headless skill delivery](issues/01-headless-skill-delivery.md) — skills reach a headless in-sandbox subagent **only via bind-mount into `~/.claude/skills/<name>`** and must be invoked by capability, never a `/`-slash command; mount kipu-code-review / kipu-spec-review / kipu-commit(+caveman-commit), inline kipu-implement's method. Auto-discovery under `-p` is unproven → spike ticket 11.
- [Credential and secrets flow](issues/04-credential-and-secrets-flow.md) — **split**: host picks the one work-item key via TS REST; the in-sandbox planner agent does all other Jira read/write/transition via MCP. **Single service-account identity** end to end (host REST basic-auth + sandbox bearer, pinned cloudId, Jira-only, English locale per ADR-0004); operator identity unused. **Secrets in a home-dir file** (`~/.config/relay/.env`, env-var override), none in the package — SA Jira token + `GITLAB_TOKEN` + Claude creds; non-secret ids (cloudId, project key, repo label) stay in `issue-tracker.md`. SA Jira token now **enters the sandbox** (MCP bearer), reversing the spike. Reshapes ticket 03's host-vs-sandbox planner framing → reconcile in ticket 07 (flow captured there).
- [Handover endpoint](issues/05-handover-endpoint.md) — pass ends by **pushing + opening a GitLab MR via `kipu-mr`**; success → item **In Review** (never Done) + resolution comment + human-readable report, exit **0** (nothing-to-do folds into 0); mid-block → **Draft MR** + `agent-blocked` label/comment, exit **1**; early bail (under-spec) → no MR + `agent-blocked` label/comment, exit **1**; error (Task-as-param / config / auth / infra) → exit **2**. `git push` location + who sets In Review deferred to ticket 07/09.
- [Per-repo configuration surface + green-gate discovery](issues/06-per-repo-config-and-green-gate.md) — config splits **two files by reader**: `relay.config.ts` (repo root, typed/zod, read by the **TS harness** — green-gate command, default branch, image ref, URL env-var defaults; no secrets, resolved from `~/.config/relay/.env`/env per ticket 04) and `docs/agents/issue-tracker.md` (read by the **in-sandbox agent**, ticket 03 stands). Green gate = **explicit command string**, relay build-tool-agnostic (runs it, reads exit code); **final gate = all tests except e2e** (migration + integration now IN → ticket 09 image must run those tiers). Package defaults (overridable): branch-prefix / 45m timeout / **per-role model map** (mirrors kipu-afk+kipu-implement: implementer+fixer+MR sonnet↑opus, fast review opus, in-depth review fable, commit+quality-gate haiku, planner opus — authoritative role list is ticket 07's). **No GitLab coords** — `glab`/`kipu-mr` derive from the remote. Validation via a **`relay doctor`** command (full opt-in check); a real run does cheap fail-fast only (config parse + secrets → exit 2), deep failures lazy but still exit 2.
- [Spike: headless skill invocation under `-p`](issues/11-headless-skill-invocation-spike.md) — **PROVEN, all three paths work headless** (claude 2.1.218): (1) **capability** — a skill mounted read-only into `~/.claude/skills/<name>` auto-fires as the `Skill` tool from a plain prompt (bare-CLI + CI-parity docker); (2) **slash expansion** — `-p "/name args"` expands client-side with zero tool_use (proven decisively via a `disable-model-invocation` command); (3) **by-name** → `Skill` tool. This **revises the prototype's "`/implement` didn't expand"** note as stale; ticket 01's "never slash" rule is **relaxed**. Two-way evidence (stream-json + effect) for each in `research/spike-11/`. **Caveat:** only *personal* artifacts tested — *plugin* marketplace skills (the real kipu-* skills) under `-p` not yet verified; build must confirm. No fallback lever needed → ticket 07 designs on solid ground.
- [Orchestration topology — the subagent graph](issues/07-orchestration-topology.md) — **harness owns the loop** (operator's persistent-planner sketch rejected: it would rebuild kipu-afk's umbrella): planner one-shot (sets In Progress, emits ordered `Output.object` plan) then TS harness iterates. **One sandbox / one branch / sequential tickets**; each role a separate `sandbox.run` (own cold session), sharing only files+git; two review lenses run concurrent read-only. Per ticket: implementer (custom prompt = `implement` skill minus review line, self-commits via `/kipu-commit`) → fast code∥spec review (opus, findings via `run-with-extraction`) → fixer. Then whole-branch in-depth code∥spec review (fable) → fixer, once. Then quality-gate (sonnet, runs+triages) → fixer, **cap 2**. Then handover (kipu-mr). **No commit role** (folds into implementer/fixer). Reviews one-shot (subjective); only the gate loops (objective). Harness array-merges findings, fixer dedups. Handoff = per-role status files (`OUTPUT_DIR`) + findings files + branch commits. **Deltas**: ticket 01 mounts gain `tdd` (not `implement`); ticket 06 model map drops `commit`, `quality-gate` haiku→sonnet.
- [Review-role spec/brief source](issues/13-review-role-spec-brief-source.md) — the **spec-review** roles fetch their intent (ticket brief / spec + ticket list) **from the tracker per `issue-tracker.md` by key** (Option B), not a planner-written file handoff — tracker stays the single source of truth; kipu-afk's `.kipu/` file convention dropped. `kipu-code-review` needs no fetch. Skill rewrite deferred. **Deltas**: ticket 04 — Jira **read** now extends to spec-review roles (same SA bearer, no new secret; writes/transitions still planner+handover); ticket 07/01 — each spec-review `sandbox.run` needs Atlassian MCP wired.
- [Packaging and distribution as an npx tool](issues/08-packaging-and-distribution.md) — one published package inverts the spike's co-located copy: compiled `dist` (ESM/`tsup`), prompts+orchestration ship as **data files in `dist`** resolved via `import.meta.url`; **target repo commits exactly two files** (`relay.config.ts` + `issue-tracker.md`), everything else carried; **flagless CLI** (`[WORK-ITEM]` | `doctor`); compiled-JS harness loads authored `relay.config.ts` via a bundled **`jiti`/`bundle-require`** loader (revises ticket 06's "TS reads TS"); manifest = exact-pinned sandcastle + zod + jiti, ESM, Node `>=20`, `files: ["dist"]`, no host `claude` dep; **publish public npmjs** (`access: public`, no `~/.npmrc` prereq — package holds no secrets). Dockerfile-home → ticket 09; publish mechanics + npmjs-vs-GitLab target → ticket 10.
- [Docker sandbox image strategy](issues/09-docker-sandbox-image.md) — image provided **prebuilt-ref-wins**: optional `image` ref in `relay.config.ts` (future staged images) → else relay **builds** from a `dockerfile` path (configurable, default `docker/relay.Dockerfile`, **never repo-root** — repo owns the CI-parity recipe; third committed file only in the build-locally case). **Baked**: JDK21+Maven, Node, native claude, glab, **+`docker` CLI**, UID/GID align. **Runtime-mounted**: skills (01/07), **Atlassian MCP config** (remote HTTP → nothing baked, carries SA bearer), and **`/var/run/docker.sock`** (docker-outside-of-Docker) — because ticket 06's gate now runs qc-catalog's **Testcontainers** integration tier, needing a live daemon; sibling containers, safe under 07's one-sandbox/sequential. **Load-bearing risk handed to the build (no ticket)**: socket mount + Testcontainers-green in a sandcastle `docker()` sandbox is **unproven** (spike ran unit-only) — build's first de-risking spike; `relay doctor` gains a socket check. **Deltas**: 08 two-file min holds only for prebuilt-ref; 06 config gains `image`+`dockerfile` fields; 01/07 mount set gains MCP config + socket; 04 bearer injects via mounted MCP config, transport confirmed remote HTTP.
- [GitLab CI build + publish pipeline](issues/10-gitlab-ci-publish.md) — `.gitlab-ci.yml` = qhub-api's, **minus the python/typescript split** (single package at root, every component's `project-dir` stays default `.`): `compile-typescript` (branch commit-test: ci→lint→test→build) + `oss-compliance-check@v1.6.3` (FOSSA, runs on branch/MR **and on tag**) on branch; `semantic-release` (main-only: bumps `package.json`, `chore(release)` commit, cuts tag); **tag pipeline** runs `oss-compliance-check` **again as the gate** (its first rule is `$CI_COMMIT_TAG` — `fossa test` fails before publish) then `publish-npm` (build + `npm publish`). All `tags: [kubernetes, cluster]`. Root `.releaserc.json` drops qhub's exec/oclif, uses `@semantic-release/npm` `npmPublish:false` to bump only. **Target = public npmjs** (resolves npmjs-vs-GitLab), **auth = OIDC trusted publishing, no `NPM_TOKEN`** (component mints `NPM_ID_TOKEN`+sigstore) — confirms 08's no-secret publish. **Setup prereqs**: configure npmjs trusted-publisher for `@quantum-hub/relay`→this project before first tag; verify `node:lts` npm ≥ 11.5 for OIDC (else override `image`). **Delta 08**: `package.json` gains `lint`/`test`/`build` scripts + `publishConfig.access:public` + Apache-2.0 `LICENSE` in `files`, commit root `package-lock.json`.
- [Pass failure and recovery semantics](issues/12-pass-failure-and-recovery-semantics.md) — **clean restart, always** (no resume/cross-pass state; the human is the recovery path — a crash leaves branch+commits for them); **branch collision → refuse, exit 2** (never delete commits relay didn't create). **Crash mid-pass → exit 2**, no MR, item left **In Progress** + best-effort Jira failure comment, **sandbox always disposed** (progress lives in host git, never leave a container up). **Needs-input never pauses** — collapses to ticket 05 mid-block (Draft MR + `agent-blocked`, exit 1); no interactive/paused state exists. **Jira transitions idempotent** — planner **ensures** In Progress (no-op if already there, via the transitions list), tolerates a re-run after crash; ticket 03 selection guards unchanged. **Deltas**: 05 exit-2 bucket gains crash + branch-collision triggers; 06 fail-fast gains a branch-collision check; 03/07 planner "sets"→"ensures" In Progress.
- [Work-item selection, type guard, and planner](issues/03-work-item-selection-and-planner.md) — planner is **config-driven** off the repo's `docs/agents/issue-tracker.md` (tracker access, repo label, relation model, issue-type mapping — repo scope sourced from the file, not the git remote); it **verifies-and-orders pre-existing tickets, never authors slices** (on-the-fly decomposition rejected). Auto-pick = frontier JQL narrowed to `issuetype in (Story, Bug, Vulnerability)` (type guard inverts the spike's child-count guard), first by priority DESC / created ASC; explicit key = same gates, no override, any failure breaks the pass. Plan = ephemeral `Output.object` list of ticket refs (has-related-tickets → those in dependency order; none → singleton), handed to the per-ticket loop; loop shape is ticket 07. Under-specified ticket ⇒ bail-to-human, never fabricate.

## Not yet specified

- Machine-readable JSON run summary for the outer poll loop — deferred here (loop is out of scope); the loop effort will want structured output beyond the exit code.

## Out of scope

- Generalizing beyond the Java/Maven pilot to the other ~15 repos / Python / TypeScript repo types — a later effort once the pilot works.
- The outer scheduling / poll loop that runs the tool repeatedly — the tool does one pass; the loop is established later.
- Driving a remote GitLab-MR CI pipeline from red to green (the spike's `ci-fix` loop) — the pass ends at the local quality gate + human handover.
